const configuracion = require('./configuracion');
const servicios = require('./servicios');
const personas = require('./personas');
const roles = require('./roles');

module.exports = {
    configuracion, servicios, personas, roles
}